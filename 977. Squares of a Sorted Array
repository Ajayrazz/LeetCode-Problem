class Solution {
    public int[] sortedSquares(int[] nums) {
        int val[] = new int[nums.length];
        for(int i=0; i<nums.length; i++){
            val[i] = nums[i] * nums[i];
        }
        Arrays.sort(val);
        return val;
    }
}
